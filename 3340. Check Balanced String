class Solution {
public:
    bool isBalanced(string num) {
        int size=num.size();
        int a=0,b=0;
        for(int i=0;i<size;i++)
        {
        if(i%2==0)
        a+=num[i]-'0';
        else
        b+=num[i]-'0';
        }
        
        return a==b;
        }
};
