class Solution {
public:
    bool checkString(string s) {
       vector<char>v;
       for(int i=0;i<s.size()-1;i++){
        if(s[i]=='b'){
            if(s[i+1]=='a')
            return false;
        }
       }
       return 1;
    }
};
